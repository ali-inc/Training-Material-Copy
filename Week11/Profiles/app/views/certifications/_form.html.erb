<div class="row">
  <div class="col-sm-6">
    <%= form_with(model: certification, local: true) do |form| %>
      <% if certification.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(certification.errors.count, "error") %> prohibited this certification from being saved:</h2>

          <ul>
          <% certification.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.label :title %>
        <%= form.text_field :title, class: "form-control", maxLength: 40, id: "title" %>
        <p>Remaining Characters: 40</p>
      </div>

      <div class="form-group">
        <%= form.label :description %>
        <%= form.text_area :description, class: "form-control", maxLength: 600, id: "description" %>
        <p>Remaining Characters: 600</p>
      </div>

      <div class="form-group">
        <%= form.submit "save" , class: "btn btn-primary", id: "save-button" %>
      </div>
    <% end %>
  </div>
</div>
