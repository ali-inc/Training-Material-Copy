<div class="row">
  <div class="col-sm-12">
    <h1 class="header">Educations</h1>
    <p>This is where your education section is displayed. All of your previously added, if any, education history will be displayed.</p>
    <p>You can add more education history by clicking on the 'Add Education'.</p>

    <table class="table">
      <thead>
        <tr>
          <th>Institution</th>
          <th>Start date</th>
          <th>End date</th>
          <th>Course</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% if @educations %>
        <%id = 0 %>
          <% @educations.each do |education| %>
            <tr>
              <td><%= link_to education.institution, edit_education_path(education), id: "title-#{id}"%></td>
              <td><%= education.start_date %></td>
              <td><%= education.end_date %></td>
              <td><%= education.course %></td>
              <td><%= link_to 'Destroy', education, method: :delete, data: { title: 'Delete Entry', confirm: 'Are you sure?', id: "destroy-#{id}" } %></td>
            </tr>
          <%id = id + 1%>
          <% end %>
        <% end %>
            <tr>
              <td class="text-center" colspan="5"><%= link_to 'Add Education', new_education_path, class: "btn btn-primary", id: "new-button" %></td>
            </tr>
      </tbody>
    </table>
  </div>
</div>
